<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>The Country list</title>
    <style>

    </style>
</head>
<body>
<main>

    <h1>Country list</h1>
    <form  method="POST">
        <fieldset>
            <label for="country">Country</label>
            <input type="text" id="country" name="country" required />
        </fieldset>

        <fieldset class="form-actions">
            <input type="submit" value="Save" />
        </fieldset>
    </form>


    {% for country in countries %}
            <div>{{country.country}}</div>
    {% else %}
        <div>There aren't countries :D</div>
    {% endfor %}
    {% for i in 0..count %}
        <a href='./some-page?offset={{ i }}'>{{ i }}</a>
    {% endfor %}

</main>
</body>
</html>

